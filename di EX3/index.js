// Store a Celsius temperature in a variable
const celsiusTemp = 25;  // Replace with your desired Celsius temperature

// Convert Celsius to Fahrenheit
const fahrenheitTemp = (celsiusTemp * 9 / 5) + 32;

// Display the result using template literals
console.log(`${celsiusTemp}°C is ${fahrenheitTemp.toFixed(2)}°F`);
